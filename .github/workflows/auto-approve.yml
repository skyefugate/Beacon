name: Auto Approve

on:
  pull_request_target:
    types: [opened, synchronize]

jobs:
  auto-approve:
    runs-on: ubuntu-latest
    if: github.event.pull_request.user.login == 'skyefugate'
    permissions:
      pull-requests: write
    steps:
      - uses: hmarr/auto-approve-action@v4
